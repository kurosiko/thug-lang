#include "thug-lang.h"
#include "./src/tokenizer/tokenizer.h"
#include "./src/test/test.h"



int main()
{
	test();
	/*
	const string str = "<html class='ahogeee'><body class='it-will-be-fine'>Test</body></html>";
	std::cout << "raw:" << str << std::endl;
	*/
	/*
	const vector<string> sprited = sprit(str);
	for (auto it = sprited.begin(); it != sprited.end();++it ) {
		
	};
	*/
	return 0;
}
